rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /user_uploads/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null; // refine with size/type checks later
    }
  }
}
